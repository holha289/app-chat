import { StyleSheet } from "react-native";

const InfoStyle = StyleSheet.create({

})


const InfoClassStyle = {
    container: 'flex-1 bg-slate-100',
    boxBlocked: 'flex-1 items-center justify-center px-4',
    boxBtn: 'px-4 mb-6',
    rowBtn: 'flex-row justify-center items-center gap-2',
}

export { InfoStyle, InfoClassStyle };